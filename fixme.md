# 当前版本v1.0.5待调整

- [x] 1. 移动端话题列表的标题显示不全，标题过长后面全是...，进入话题详情也一样。希望能做成外面多行显示标题(可以控制最多3行)，里面在标题页支持多行下把标题显示完整(注意管理员操作话题置顶加精等按钮可能占据标题的同一行，想办法优化)。可以限制话题标题总的不超过50个字
- [x] 2. 文件列表的打标签功能，系统里没配置的标签，如果之前有，现在后台设置取消了，那打标签想取消这个会报错。比如最初有cat标签，后面系统管理改成了catvod，现在用户去打标签取消之前的cat标签没法保存上，还得系统管理把这个加回来，用户才能取消掉。
- [x] 3. 话题评论的回复按钮图标有歧义，还以为是撤回按钮，需要换一个合适的图标
- [x] 4. 文件列表的标签筛选需要支持多选，现在只是单选。另外应该再增加密这个标签，方便筛选到特殊源
- [ ] 5. 话题评论现在只支持删除了重新发，考虑是否增加编辑现有评论功能？文件列表的文件也是不能编辑只能删了重新传。这个功能必要性不是很高，后面再看看
- [x] 6. 精华话题应该跟置顶话题一样有个标签，不然一眼看不出来
- [x] 7. 后面话题列表数据多了之后，需要考虑做翻页功能和话题搜索功能，下方增加翻页组件
- [x] 8. 聊天室没有艾特用户功能。可以不要私聊功能，但是需要艾特用户。用户最好能看到红点，点击滚动到那条消息去。论坛评论和话题内容也要支持艾特用户，这种需要给用户发系统消息通知，用户能点击调到对应的话题或者评论位置
- [x] 9. 文件列表加个序号字段，并冻结列，动态根据当前文件列表数量计算序号，从1开始。尽量占最少的空间。把标签字段移动到文件名后方。
- [x] 10. 检查一下注册问题，注册时提示注册限制已超出，看看代码里有没有逻辑问题，用户并没有超出单ip注册限制
- [x] 11. 移动端文件列表文件名前面归档的图标取消节省空间。
- [x] 12. 后台管理用户管理需要加用户数量统计
